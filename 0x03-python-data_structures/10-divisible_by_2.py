#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    for i in range(0, len(my_list)):
        check = my_list[i]
        if check % 2 == 0:
            return True
        else:
            return false
